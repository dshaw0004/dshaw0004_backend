import os
from backend.constants import ROOT_DIR
# import firebase_admin
# from firebase_admin import credentials
print(ROOT_DIR)

# cred3 = credentials.Certificate(f"{ROOT_DIR}appbank_config.json")
# app3 = firebase_admin.initialize_app(cred2, name="appbank")
